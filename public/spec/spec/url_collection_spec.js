describe("jetfuelexpress.UrlCollection", function() {
  it('sorts things properly', function() {
    // make fake xhr with sinon
    // ...and return what /api/urls would normally return
    // {url: {name: 'Blah', url: 'http://google.com'}}

    // init collection
    // fetch data

    // add an item to the collection and make sure it's still order

    // expect(collection.at(0).get('name')).toBe('Blah')
    // expect(collection.at(1).get('name')).toBe('Foo')


  })
});
